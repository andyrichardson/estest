# Usage

```
# Model 1
docker-compose up es0
docker-compose run model1 npm i
docker-compose run model1 node index.mjs

# Cleanup
docker-compose down

# Model 2
docker-compose up es0
docker-compose run model2 npm i
docker-compose run model2 node index.mjs
```
